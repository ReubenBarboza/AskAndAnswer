import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  ModerateQuestionContainer: {
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
    width: "75%",
  },
});
